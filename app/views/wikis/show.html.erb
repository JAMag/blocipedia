<h1><%= markdown_to_html @wiki.title %></h1>

<% if policy(@wiki).edit? %>
  <%= link_to "Darth-Wiki Edit", edit_wiki_path(@wiki), class: 'btn btn-success' %>
<% end %>
<div class="row"
  <div class="col-md-8">
<p><%= markdown_to_html @wiki.body %></p>
</div>